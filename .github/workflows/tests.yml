name: tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pytest:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # 3.12 is what the shared em-lib environment uses; 3.11 is the floor
        # pyproject declares, and testing it is what keeps that honest.
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v7

      # Sibling packages, not on PyPI. Both are public, so no token is needed — but pip
      # would still try PyPI for the `em-volume-tools` requirement, so they have to be
      # installed from source FIRST, and in dependency order.
      - uses: actions/checkout@v7
        with:
          repository: flatiron-connectomics/em-blockrun
          path: .siblings/em-blockrun

      - uses: actions/checkout@v7
        with:
          repository: flatiron-connectomics/em-volume-tools
          path: .siblings/em-volume-tools

      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      # No conda, as in every repo here. That costs this package nothing: `layers` and
      # `state` are pure dict manipulation and need no store at all, and the volume-reading
      # tests build real local volumes through tensorstore, which pip provides.
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install ./.siblings/em-blockrun ./.siblings/em-volume-tools
          pip install -e ".[dev]"

      - name: Test
        run: pytest -q

      - name: Smoke-test the installed console script
        # The suite runs from a source tree, so it cannot catch a packaging fault that only
        # shows up once installed — which is the whole reason this package has a CLI. Run it
        # from a directory with no checkout in sight.
        run: |
          pip install build
          python -m build --wheel -o dist/
          pip install --force-reinstall --no-deps dist/*.whl
          cd /tmp && em-ngl --help > /dev/null
          cd /tmp && em-ngl gen --help > /dev/null
          cd /tmp && em-ngl shaders > /dev/null
          cd /tmp && python -c "
          import em_ngl, sys
          # The lazy top-level exports must not drag em-volume-tools in: `cli` reads
          # __version__ from __init__, so an eager import there would slow every --help.
          assert 'em_volume_tools' not in sys.modules, 'importing em_ngl pulled in em_volume_tools'
          assert hasattr(em_ngl, 'build_state')
          print('lazy exports resolve; version', em_ngl.__version__)
          "

  # Ask the documentation site to rebuild, so the published CLI reference follows this
  # commit. `main` only, and only once the tests pass: the reference is generated from the
  # live parsers, so publishing from a red commit would document code nobody should run.
  docs:
    needs: pytest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Rebuild flatiron-connectomics/em-libraries
        env:
          # The workflow's own GITHUB_TOKEN is scoped to THIS repository and cannot
          # dispatch to another one, so this needs a separate token with contents:write on
          # em-libraries. When it is absent the step says so and succeeds — a docs refresh
          # that did not happen must not turn a green test run red, and the site rebuilds
          # on its nightly schedule regardless.
          TOKEN: ${{ secrets.DOCS_DISPATCH_TOKEN }}
        run: |
          if [ -z "$TOKEN" ]; then
            echo "::notice::DOCS_DISPATCH_TOKEN is not set; skipping. The site will pick this commit up on its next scheduled build."
            exit 0
          fi
          curl -fsS -X POST \
            -H "Authorization: Bearer $TOKEN" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/flatiron-connectomics/em-libraries/dispatches \
            -d "{\"event_type\":\"package-updated\",\"client_payload\":{\"repo\":\"$GITHUB_REPOSITORY\",\"sha\":\"$GITHUB_SHA\"}}"
          echo "::notice::asked em-libraries to rebuild its docs for $GITHUB_SHA"
